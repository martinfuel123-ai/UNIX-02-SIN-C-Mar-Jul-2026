bash --version
env
echo $SHELL
echo $RANDOM
echo $UID
echo $OSTYPE
ps -ef
df --human-readable 
#!/usr/bin/env bash
#!/bin/bash
# Run script in debug mode (shows each command before executing)
bash -x blackhatbash1.sh
# Run bash with restricted mode (NOT normally used for scripts like this)
bash -r blackhatbash1.sh
